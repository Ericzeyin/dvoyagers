@model IEnumerable<IEProject1.Models.Content>
@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout_Semantic.cshtml";
}

<!-- Following Menu -->
<div class="ui large top fixed menu">
    <div class="ui container">
        <a href="@Url.Action("Index", "Contents")" class="item">Home</a>
        @*<a href="@Url.Action("Edit", "Contents", new { id= 1 })" class="item">Sports</a>
        <a href="@Url.Action("Edit", "Contents", new { id= 2 })" class="item">Religion</a>
        <a href="@Url.Action("Edit", "Contents", new { id= 3 })" class="item">Yoga</a>
        <a href="@Url.Action("Edit", "Contents", new { id= 4 })" class="item">Culture</a>
        <a href="@Url.Action("Create", "Contents")" class="item">Statistics</a>*@
        <a href="@Url.Action("Index", "Places")" class="item">Sports</a>
        <a href="@Url.Action("Index", "RPlaces")" class="item">Religion</a>
        <a href="@Url.Action("Index", "YPlaces")" class="item">Yoga&Taichi</a>
        <a href="@Url.Action("Edit", "Contents", new { id= 4 })" class="item">Culture</a>
        @*<div class="right menu">
        <div class="item">
            <a class="ui button">Log in</a>
        </div>
        <div class="item">
            <a class="ui primary button">Sign Up</a>
        </div>*@
        @*</div>*@
    </div>
</div>

@*<div class="ui grid container">
        @foreach (var item in Model)
        {
            <div class="four wide column ui card">
                <div class="image">
                    <img src="~/Content/Images/@item.ImageName">
                </div>
                <div class="content">
                    <a class="header">@Html.DisplayFor(modelItem => item.ContentName)</a>
                    <div class="description">
                        @Html.DisplayFor(modelItem => item.Comment)
                    </div>
                </div>
                <div class="extra content">
                    <a>
                        <i class="user icon"></i>
                        The rank of this activity is: @Html.DisplayFor(modelItem => item.Rank)
                    </a>
                </div>
            </div>
        }
    </div>*@
<div class="ui grid container">
    <div class="ui four cards">
        @foreach (var item in (IEnumerable<IEProject1.Models.Content>)ViewData["content"])
        {
            <div class="card">
                <div class="image">
                    <img src="~/Content/Images/@item.ImageName">
                </div>
                <div class="extra">
                    Rating:
                    <div class="ui rating" data-rating="@item.Popularity" data-max-rating="5"></div>
                </div>
            </div>
        }
    </div>


 </div>



